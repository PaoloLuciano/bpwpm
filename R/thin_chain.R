thin_chain <- function(){
    

    "Generic Function that simplifies the matrix of a draw
    thin:= Thinning parameter for the Gibbs Sampler (integer - thin >= 0)
            burn_in:= Burn In parameter (integer - burn_in < draw)"
    
    
}